export async function processAudio(blob,{onProgress=()=>{}}={}){onProgress('validating');if(!blob||blob.size<1024)throw Error('Invalid audio payload');const type=blob.type||'';if(type==='audio/flac'||type==='audio/x-flac'||/\.flac$/i.test(blob.name||'')){return new Blob([blob],{type:'audio/flac'})}onProgress('transcode-placeholder');throw Error('Audio transcoding is not configured yet: install a bundled ffmpeg.wasm build before enabling non-FLAC providers')}
export async function sha256(blob){const digest=await crypto.subtle.digest('SHA-256',await blob.arrayBuffer());return [...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,'0')).join('')}